# ============================================================================
# 仓库根级忽略规则
# 策略：本仓库只跟踪 **代码 / 配置 / 文档**；
#       模型权重、数据缓存、训练与推理产物等大文件一律不入库。
# 注：历史提交中已存在的权重仍由 Git LFS 托管。.gitignore 对**已跟踪**文件
#     不生效，因此它们会继续留在仓库中（如需彻底清除需单独处理）。
# ============================================================================

# ---- 编辑器 / 系统 ----
.idea/
.vscode/
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.tmp

# ---- Python 字节码与工具缓存 ----
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# ---- 平台文档目录（按需求排除，不入库）----
docs/

# ---- 模型权重（不再入库）----
*.pth
*.pt
*.ckpt
*.onnx
*.h5
*.pkl
*.safetensors

# ---- 医学影像与数据缓存（不再入库）----
*.npz
*.nii
*.nii.gz
*.dcm
*.mha
*.mhd

# ---- 训练 / 推理产物（不再入库）----
runs/
cache/
checkpoints/
logs/
*.jsonl
*.log
